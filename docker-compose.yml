services:
  api:
    build: .
    ports:
      - "8000:8000"
    env_file:
      - ./.env.docker
    depends_on:
      - db

  db:
    image: postgres:17
    env_file:
      - ./.env.docker
    volumes:
      - postgres_data:/var/lib/postgresql/data

volumes:
  postgres_data:
